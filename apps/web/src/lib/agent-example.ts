export function agentExamplePrompt(site = "this Jobbbler site"): string {
  return `Open ${site}. Looking for a Product Designer position, remote, $90k+. Compare options, pick the top 2, and apply. Schedule for daily updates (every morning). My CV is attached.`;
}
